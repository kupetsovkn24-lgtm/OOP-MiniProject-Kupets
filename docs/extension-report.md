# Extension Report - IW29

## Розширення A: гнучкий фільтр

| Пункт | Опис |
| --- | --- |
| Вхідний артефакт | `JobQueryExtensions` із базовими LINQ-запитами |
| Зміна | Додано `FilterCompletedJobs(..., Func<Job, bool> condition)` |
| Результат | Умова відбору передається як лямбда, а незавершені роботи автоматично виключаються |
| Підготовка наступного кроку | Report може повторно використати один і той самий фільтр для різних порогів |

## Розширення B: звіт дорогих ремонтів

| Пункт | Опис |
| --- | --- |
| Вхідний артефакт | Фільтр завершених робіт із кроку A |
| Зміна | Додано `CompletedJobsReport`, `BuildCompletedJobsReport(...)` і `WorkshopFacade.GetExpensiveCompletedJobsReport(...)` |
| Результат | Застосунок отримує список відібраних ремонтів, їх кількість і загальну суму |
| Підготовка наступного кроку | Готовий звіт можна показувати користувачу та перевіряти тестами |

## Розширення C: демонстрація і тести

| Пункт | Опис |
| --- | --- |
| Вхідний артефакт | Звіт із кроку B |
| Зміна | У команду статистики Console додано вивід ремонтів від `4000 UAH`; додано тести extensions і facade |
| Результат | Демо-сценарій показує звіт, а тести перевіряють дорогу, дешеву, незавершену та порожню вибірку |
| Підготовка наступного кроку | Функціональність готова для пояснення на захисті |

## Що це дало проєкту

- Делегати використовуються для корисної гнучкої поведінки, а не тільки для технічного callback.
- LINQ-запити тепер формують невеликий приклад бізнес-звіту.
- Нова поведінка не зачіпає persistence або правила переходу статусів.

## Перевірка

- Build: успішно, `0` warnings і `0` errors.
- Tests: `62/62` пройшли.
- Coverage: `90.79%` lines (`592/652`), `61.11%` branches (`110/180`).
- Console demo: звіт для порога `4000 UAH` показує `1` роботу та `4700 UAH`.
